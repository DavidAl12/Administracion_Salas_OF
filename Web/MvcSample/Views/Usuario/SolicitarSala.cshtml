@model Servicios.Models.SolicitarSalaViewModel

<h2>Solicitar Sala Completa</h2>
<form asp-action="SolicitarSala" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="mb-3">
        <label>Sala</label>
        <select asp-for="SalaId" class="form-control">
            <option value="">Selecciona una sala</option>
            @foreach (var sala in Model.SalasDisponibles)
            {
                <option value="@sala.Id">@sala.Nombre</option>
            }
        </select>
        <span asp-validation-for="SalaId" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label>Fecha y Hora de Inicio</label>
        <input asp-for="FechaInicio" class="form-control" type="datetime-local" />
        <span asp-validation-for="FechaInicio" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label>Fecha y Hora de Fin</label>
        <input asp-for="FechaFin" class="form-control" type="datetime-local" />
        <span asp-validation-for="FechaFin" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Enviar Solicitud de Sala</button>
</form>
